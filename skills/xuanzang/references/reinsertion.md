# Reinsertion

DOCX assembly follows canonical chapter order and image markers. EPUB reinsertion replaces text nodes by stored anchors; do not use fuzzy matching for publication output.

Preserve image paths, attributes, anchors, hrefs, note references, OPF, spine, and nav. Validate XHTML after reinsertion.
