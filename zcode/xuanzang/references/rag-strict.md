# RAG Strict

RAG strict output must include `structure.json`, `sections/`, `audit/pass_fail.json`, source ledger, and cleaning audit.

PASS_STRICT requires no active source coverage gap, OCR corruption, low-confidence boundary, false-pass issue, missing image, or corrupted title tree.
